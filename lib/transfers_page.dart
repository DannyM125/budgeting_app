import 'package:flutter/material.dart';

class TransfersPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Transfers')),
      body: Center(child: Text('Transfers Page Content')),
    );
  }
}